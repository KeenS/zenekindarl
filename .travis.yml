language: lisp
env:
  matrix:
    - LISP=sbcl

matrix:
  allow_failures:

install:
  - curl -L https://github.com/luismbo/cl-travis/raw/master/install.sh | sh
  - ql install arrows
script:
  - cl -e "(require 'asdf)" -e "(require 'arrows-test)" -e "(unless (prove:run :arrows-test) (uiop:quit 1))"